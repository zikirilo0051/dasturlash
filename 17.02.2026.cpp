#include  <iostream>
#include <vector>
#include <algoritim>
#include <cmath> 
using namespace std;
int main(){ 
int current_qavat = 7;
vector<int> requests = {2, 9, 1};
vector<int> tepaga, pastga;
for (int qavat : sorovlar) {
	if (qavat > hozirgi_qavat
{
	tepaga.push_back(qavat);
} else {
	pastga.push_back(floor);
}
}
saralash(tepaga.boshlash(), tepaga.tugadi());
saralsh(pastga.boshlash(), pastga.tugadi(),
greatear<int>());
cout << "liftning harakatlanish tartibi: "pastga. tugadi"
}